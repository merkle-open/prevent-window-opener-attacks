import uglify from "rollup-plugin-uglify";

export default {
  entry: "src/lib.js",
  format: "umd",
  moduleName: "preventWindowOpenAttacks",
  plugins: [uglify()],
  sourceMap: true,
  dest: "dist/lib.min.js"
};
